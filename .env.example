# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=goldshop

# Server port
PORT=4000

# Admin simple login (no JWT, just basic check)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Points rule: how many currency units = 1 point (e.g. 100 => RM100 = 1 point)
POINTS_RATE=100
